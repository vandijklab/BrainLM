import pytest
from datasets import load_from_disk


class TestDataLoading:
    def test_load_fmri_data(self):
        # ds = load_from_disk("datasets/one_batch_perturbseq_cell_dataset")
        # assert ds.num_rows == 32
        # assert len(ds["cell_expression_vector"][0]) == 8248
        # TODO: fmri dataloading test
        pass
